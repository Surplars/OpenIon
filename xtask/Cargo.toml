[package]
name = "xtask"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "xtask"
test = false
bench = false

[dependencies]
anyhow = "1.0"
clap = { version = "4", features = ["derive"] }
ionix = { path = "../utils/ionix" }
toml = "1"
